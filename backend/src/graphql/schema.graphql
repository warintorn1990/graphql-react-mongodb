type Query {
    ping: String!
    message: [Message!]
}

type Mutation {
    createMessage(title: String!, content: String!, author: String!): Message
}

type Message {
    _id: ID!
    title: String!
    content: String!
    author: String!
}

